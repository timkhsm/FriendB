# Telegram Chat-Bot (Python, Telebot, ML)

Этот проект — интерактивный Telegram-бот на Python, который умеет:

- вести диалог с пользователем,
- генерировать ответы на основе обученной модели (Logistic Regression + CountVectorizer),
- отправлять фотографии и музыку,
- получать информацию из Википедии,
- обучаться новым диалогам («коррекция ответа»),
- работать через удобные кнопки клавиатуры Telegram.

Проект реализован с использованием `pyTelegramBotAPI`, `scikit-learn`, `wikipedia`, `BeautifulSoup` и стандартных инструментов Python.

---

## Функционал

### **Генерация ответов**
Бот читает датасет `dialogues.txt`, очищает текст и обучает модель:

- `CountVectorizer` — векторизация текста,
- `LogisticRegression` — предсказание ответа.

Бот автоматически обновляет модель при появлении новых данных.

---

### **Ответы из Википедии**
При необходимости бот может:

1. найти страницу в Википедии,
2. очистить текст,
3. вернуть краткую, адаптированную выдержку.

---

### **Самообучение**
Если пользователь выбирает *«Хотел другого ответа🥲»*, бот:

- спрашивает правильный ответ,
- записывает пару `вопрос — ответ` в `dialogues.txt`,
- автоматически переобучается.

---

### **Фото и музыка**
На основе выбора пользователя бот отправляет:

- фотографии (котята, море, закат),
- музыку и изображения альбомов.

Все пути к файлам задаются в `config.py`.

---

### **Интерактивные кнопки**
Бот использует `ReplyKeyboardMarkup`, чтобы:

- вести диалог сценарием,
- давать выбор действий,
- упрощать взаимодействие.

---

## Структура проекта

```plaintext
project/
├── bot.py              # основной код бота
├── config.py           # токен и пути к медиа
├── dialogues.txt       # датасет диалогов
├── requirements.txt    # зависимости (опционально)
└── README.md           # описание проекта (вы читаете его)
